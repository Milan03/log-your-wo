/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * Add any extra browser polyfills here.
 *
 * The application runs zoneless (provideZonelessChangeDetection in
 * app.config.ts), so zone.js is intentionally NOT loaded. The unit tests still
 * use a zone-based TestBed and load zone.js + zone.js/testing via the test
 * target's `polyfills` array in angular.json.
 *
 * The current setup targets "evergreen" browsers (the last versions of
 * self-updating browsers). Learn more: https://angular.io/guide/browser-support
 */
